# python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# generated fast-sim outputs (reproduce with fastsim/scripts/phase_space_map.py)
fastsim/out/

# local copies of public reference papers (large; fetch from arXiv instead)
/athena.pdf
/jam.pdf

# editor swap files
*.swp
*.swo
.*.sw?

# external / upstream code trees (do not commit; use a submodule if needed)
BeAGLE_jz/

# generated evgen outputs (reproduce with evgen/scripts/closure_fom.py)
evgen/out/

# generated coverage plot directory (not tracked; regenerate from scripts)
fastsim/scripts/out_coverage/

# macOS Finder metadata
.DS_Store

# reference papers: the PDFs in refs/ ARE tracked (index in refs/README.md,
# dictionary refs/refs_dict.json).  Only the unsplit EIC Yellow Report is
# ignored (124 MB > GitHub's 100 MB limit; it is committed as four parts).
refs/2103.05419.pdf

# hadronic-final-state generator samples (large; regenerate with tools/pythia8/gen_dis_hfs.py)
evgen/samples/*.npz
