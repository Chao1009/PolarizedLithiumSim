# Evidence dumps written by a failing test (tests/test_recopseudo.py's
# _evidence_on_failure): the compared arrays plus the machine state of the
# run.  Diagnostic only, regenerated by the next failure, never committed.
tests/_failures/
